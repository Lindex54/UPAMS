@extends('layouts.app')
@section('title','Record Payment | Property Management') @section('portal-label','Finance & Utilities') @section('page-heading','Record payment')
@section('content')
@include('admin.partials.flash')
<form method="POST" action="{{ route('payments.store') }}" class="mx-auto max-w-3xl rounded-2xl border border-border bg-white p-6 shadow-sm dark:bg-slate-900">@csrf
<p class="mb-5 text-sm text-body-text">The invoice balance is checked again while saving to prevent duplicate or excess payments.</p><div class="grid gap-5 md:grid-cols-2"><label class="text-sm font-semibold md:col-span-2">Invoice *<select name="invoice_id" class="mt-2 w-full rounded-lg border border-border bg-transparent p-3"><option value="">Select open invoice</option>@foreach($invoices as $invoice)<option value="{{ $invoice->id }}" @selected(old('invoice_id',$selectedInvoice)==$invoice->id)>{{ $invoice->reference }} — {{ $invoice->beneficiary->full_name_organization }} — UGX {{ number_format($invoice->balance(),2) }}</option>@endforeach</select></label><label class="text-sm font-semibold">Paid at *<input type="datetime-local" name="paid_at" value="{{ old('paid_at',now()->format('Y-m-d\TH:i')) }}" class="mt-2 w-full rounded-lg border border-border bg-transparent p-3"></label><label class="text-sm font-semibold">Amount (UGX) *<input type="number" min="0.01" step="0.01" name="amount" value="{{ old('amount') }}" class="mt-2 w-full rounded-lg border border-border bg-transparent p-3"></label><label class="text-sm font-semibold">Payment method *<select name="payment_method" class="mt-2 w-full rounded-lg border border-border bg-transparent p-3">@foreach(['Cash','Bank Transfer','Mobile Money','Cheque','Card','Other'] as $method)<option @selected(old('payment_method')===$method)>{{ $method }}</option>@endforeach</select></label><label class="text-sm font-semibold">External payment reference<input name="payment_reference" value="{{ old('payment_reference') }}" class="mt-2 w-full rounded-lg border border-border bg-transparent p-3"></label><label class="text-sm font-semibold md:col-span-2">Notes<textarea name="notes" class="mt-2 w-full rounded-lg border border-border bg-transparent p-3">{{ old('notes') }}</textarea></label></div><div class="mt-6 flex justify-end gap-3"><a href="{{ route('payments.index') }}" class="rounded-lg border border-border px-5 py-3">Cancel</a><button class="cursor-pointer rounded-lg bg-busitema-gold px-5 py-3 font-bold text-slate-950">Record and generate receipt</button></div></form>
@endsection
