@extends('layouts.app')
@section('title','Register Utility Meter | Property Management') @section('portal-label','Finance & Utilities') @section('page-heading','Register utility meter')
@section('content')
@include('admin.partials.flash')
<form method="POST" action="{{ route('utilities.meters.store') }}" class="mx-auto max-w-4xl rounded-2xl border border-border bg-white p-6 dark:bg-slate-900">@csrf<div class="grid gap-5 md:grid-cols-2"><label class="text-sm font-semibold">Utility type *<select name="utility_type_id" class="mt-2 w-full rounded-lg border border-border bg-transparent p-3">@foreach($types as $type)<option value="{{ $type->id }}">{{ $type->name }} ({{ $type->unit }})</option>@endforeach</select></label><label class="text-sm font-semibold">Campus *<select name="campus_id" class="mt-2 w-full rounded-lg border border-border bg-transparent p-3">@foreach($campuses as $campus)<option value="{{ $campus->id }}">{{ $campus->name }}</option>@endforeach</select></label><label class="text-sm font-semibold">Beneficiary (optional)<select name="beneficiary_id" class="mt-2 w-full rounded-lg border border-border bg-transparent p-3"><option value="">University / unassigned</option>@foreach($beneficiaries as $beneficiary)<option value="{{ $beneficiary->id }}">{{ $beneficiary->full_name_organization }}</option>@endforeach</select></label>@foreach(['property_reference'=>'Property reference','meter_number'=>'Meter number *','unit'=>'Unit *'] as $name=>$label)<label class="text-sm font-semibold">{{ $label }}<input name="{{ $name }}" value="{{ old($name) }}" @required(str_contains($label,'*')) class="mt-2 w-full rounded-lg border border-border bg-transparent p-3"></label>@endforeach<label class="text-sm font-semibold">Rate (UGX per unit) *<input type="number" step="0.0001" min="0" name="rate" value="{{ old('rate') }}" class="mt-2 w-full rounded-lg border border-border bg-transparent p-3"></label><label class="text-sm font-semibold">Abnormal consumption threshold<input type="number" step="0.0001" min="0" name="abnormal_threshold" value="{{ old('abnormal_threshold') }}" class="mt-2 w-full rounded-lg border border-border bg-transparent p-3"></label><label class="text-sm font-semibold">Status *<select name="status" class="mt-2 w-full rounded-lg border border-border bg-transparent p-3"><option>Active</option><option>Inactive</option><option>Under Maintenance</option></select></label></div><div class="mt-6 text-right"><button class="rounded-lg bg-busitema-gold px-5 py-3 font-bold text-slate-950">Register meter</button></div></form>
@endsection
