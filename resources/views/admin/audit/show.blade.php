@extends('layouts.app')
@section('title','Audit Event | UPAMS') @section('portal-label','Governance') @section('page-heading','Audit event #'.$auditLog->id)
@section('content')
<article class="rounded-2xl border border-border bg-white p-6 dark:bg-slate-900"><dl class="grid gap-4 sm:grid-cols-3">@foreach(['Action'=>$auditLog->action,'Actor'=>$auditLog->user_name ?? 'System','Created At'=>$auditLog->created_at->format('d M Y H:i:s'),'Record'=>class_basename($auditLog->auditable_type ?? '').' #'.$auditLog->auditable_id,'IP Address'=>$auditLog->ip_address,'User Agent'=>$auditLog->user_agent] as $label=>$value)<div><dt class="text-xs uppercase text-body-text">{{ $label }}</dt><dd class="break-words font-semibold">{{ $value ?: '—' }}</dd></div>@endforeach</dl><p class="mt-6 rounded-xl bg-slate-50 p-4 dark:bg-slate-800">{{ $auditLog->description }}</p><div class="mt-6 grid gap-4 md:grid-cols-2"><div><h3 class="font-bold">Previous values</h3><pre class="mt-2 overflow-auto rounded-xl bg-slate-950 p-4 text-xs text-slate-100">{{ json_encode($auditLog->old_values, JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES) ?: '{}' }}</pre></div><div><h3 class="font-bold">New values</h3><pre class="mt-2 overflow-auto rounded-xl bg-slate-950 p-4 text-xs text-slate-100">{{ json_encode($auditLog->new_values, JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES) ?: '{}' }}</pre></div></div></article>
@endsection
