@extends('layouts.app')
@section('title','Audit Trail | UPAMS') @section('portal-label','Governance') @section('page-heading','Audit Trail')
@section('content')
<div class="rounded-2xl border border-border bg-white p-5 dark:bg-slate-900"><div class="rounded-xl border border-amber-300 bg-amber-50 p-4 text-sm text-amber-900 dark:border-amber-800 dark:bg-amber-950 dark:text-amber-200">Audit records are permanent and read-only, including for the System Administrator.</div><form class="mt-4 grid gap-2 md:grid-cols-4"><input name="q" value="{{ request('q') }}" class="rounded-lg border border-border bg-transparent p-2" placeholder="User or description"><select name="action" class="rounded-lg border border-border bg-transparent p-2"><option value="">All actions</option>@foreach($actions as $action)<option @selected(request('action')===$action)>{{ $action }}</option>@endforeach</select><select name="campus_id" class="rounded-lg border border-border bg-transparent p-2"><option value="">All campuses</option>@foreach($campuses as $campus)<option value="{{ $campus->id }}" @selected(request('campus_id')==$campus->id)>{{ $campus->name }}</option>@endforeach</select><button class="rounded-lg bg-busitema-blue p-2 font-semibold text-white">Filter</button></form><div class="mt-3 text-right"><a href="{{ route('audit-trail.export',request()->query()) }}" class="rounded-lg border border-border px-4 py-2">Export CSV</a></div><div class="mt-5 overflow-x-auto"><table class="w-full text-left text-sm"><thead><tr class="border-b border-border text-xs uppercase text-body-text"><th class="p-3">Created At</th><th class="p-3">Actor / campus</th><th class="p-3">Action</th><th class="p-3">Record</th><th class="p-3">Description</th><th class="p-3">IP</th></tr></thead><tbody>@forelse($logs as $log)<tr class="border-b border-border"><td class="p-3">{{ $log->created_at->format('d M Y H:i:s') }}</td><td class="p-3">{{ $log->user_name ?? 'System' }}<br><span class="text-xs">{{ $log->campus?->name ?? 'University-wide' }}</span></td><td class="p-3"><a href="{{ route('audit-trail.show',$log) }}" class="font-semibold text-busitema-blue dark:text-sky-300">{{ $log->action }}</a></td><td class="p-3 text-xs">{{ class_basename($log->auditable_type ?? '') }} #{{ $log->auditable_id }}</td><td class="p-3">{{ $log->description }}</td><td class="p-3">{{ $log->ip_address }}</td></tr>@empty<tr><td colspan="6" class="p-8 text-center">No audit records match.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $logs->links() }}</div></div>
@endsection
