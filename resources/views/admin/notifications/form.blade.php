@extends('layouts.app')
@section('title','Compose Notification | UPAMS') @section('portal-label','Governance') @section('page-heading','Compose notification')
@section('content')
@include('admin.partials.flash')
<form method="POST" action="{{ route('notifications.store') }}" class="mx-auto max-w-4xl rounded-2xl border border-border bg-white p-6 dark:bg-slate-900" x-data="{template:''}">@csrf<p class="mb-5 text-sm text-body-text">In-system delivery is active. Email and SMS channel selections are retained for future connector integration.</p><div class="grid gap-5 md:grid-cols-2"><label class="text-sm font-semibold">Template<select name="notification_template_id" class="mt-2 w-full rounded-lg border border-border bg-transparent p-3"><option value="">No template</option>@foreach($templates as $template)<option value="{{ $template->id }}">{{ $template->name }}</option>@endforeach</select></label><label class="text-sm font-semibold">Event type *<select name="event_type" class="mt-2 w-full rounded-lg border border-border bg-transparent p-3">@foreach(['Expiry','Arrears','Servicing','Calibration','Inspection','Maintenance','Approval','General'] as $type)<option>{{ $type }}</option>@endforeach</select></label><label class="text-sm font-semibold">Specific user<select name="recipient_user_id" class="mt-2 w-full rounded-lg border border-border bg-transparent p-3"><option value="">None</option>@foreach($users as $user)<option value="{{ $user->id }}">{{ $user->name }}</option>@endforeach</select></label><label class="text-sm font-semibold">Campus target<select name="campus_id" class="mt-2 w-full rounded-lg border border-border bg-transparent p-3"><option value="">None</option>@foreach($campuses as $campus)<option value="{{ $campus->id }}">{{ $campus->name }}</option>@endforeach</select></label><label class="text-sm font-semibold">Role target<select name="target_role" class="mt-2 w-full rounded-lg border border-border bg-transparent p-3"><option value="">None</option>@foreach($roles as $role)<option>{{ $role->name }}</option>@endforeach</select></label><label class="text-sm font-semibold">Title *<input name="title" required class="mt-2 w-full rounded-lg border border-border bg-transparent p-3"></label><label class="text-sm font-semibold md:col-span-2">Message *<textarea name="message" required rows="5" class="mt-2 w-full rounded-lg border border-border bg-transparent p-3"></textarea></label><fieldset class="md:col-span-2"><legend class="font-semibold">Channels *</legend><div class="mt-2 flex gap-5"><label><input type="checkbox" name="channels[]" value="in_system" checked> In-system</label><label><input type="checkbox" name="channels[]" value="email"> Email later</label><label><input type="checkbox" name="channels[]" value="sms"> SMS later</label></div></fieldset></div><div class="mt-6 text-right"><button class="rounded-lg bg-busitema-gold px-5 py-3 font-bold text-slate-950">Send notification</button></div></form>
@endsection
