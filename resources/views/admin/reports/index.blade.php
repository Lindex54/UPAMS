@extends('layouts.app')
@section('title','Report Centre | Property Management') @section('portal-label','Governance') @section('page-heading','Report Centre')
@section('content')
<div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">@foreach($metrics as $label=>$value)<div class="rounded-2xl border border-border bg-white p-5 dark:bg-slate-900"><p class="text-xs uppercase text-body-text">{{ $label }}</p><p class="mt-2 text-2xl font-bold">{{ is_numeric($value) ? number_format($value, str_contains($label,'UGX') ? 2 : 0) : $value }}</p></div>@endforeach</div><div class="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">@foreach($reports as $key=>$title)<a href="{{ route('reports.show',$key) }}" class="rounded-2xl border border-border bg-white p-5 transition hover:border-busitema-blue dark:bg-slate-900"><p class="text-xs uppercase text-busitema-blue dark:text-sky-300">University-wide report</p><h2 class="mt-2 text-lg font-bold">{{ $title }}</h2><p class="mt-2 text-sm text-body-text">Filter by campus, print to PDF, or export a spreadsheet-compatible CSV.</p></a>@endforeach</div><div class="mt-6 rounded-xl border border-border p-4 text-sm text-body-text">Asset, land, building, laboratory/equipment, vehicle, agreement, maintenance, inspection, and document-expiry report tiles will automatically become data-backed as those presentation-only modules receive database registries. This centre does not fabricate figures for registries that do not yet exist.</div>
@endsection
