@extends('layouts.app')
@php
    $titles=['assignments'=>'Equipment Assignments','maintenance'=>'Maintenance & Repairs','inspections'=>'ICT Inspections','transfers'=>'Equipment Transfers','documents'=>'Technical Documents','reports'=>'ICT Reports'];
    $input='mt-1.5 w-full rounded-lg border border-border bg-white px-3 py-2.5 text-heading outline-none focus:border-busitema-blue';
@endphp
@section('title',$titles[$page].' | Property Management') @section('portal-label',auth()->user()->role?->name === 'System Administrator' ? 'Property Management Administration · ICT Assets' : 'IT Technician Workspace') @section('page-heading',$titles[$page]) @section('user-role',auth()->user()->role?->name??'IT Technician')
@section('sidebar') @include('technician.partials.sidebar') @endsection
@section('content')
<div class="space-y-6" x-data="{ formOpen: false }">@include('technician.partials.messages')
@if($page==='assignments')
<div class="flex justify-end"><button @click="formOpen=!formOpen" class="rounded-lg bg-busitema-blue px-4 py-2.5 text-sm font-semibold text-white">Assign / Reassign Equipment</button></div>
<form x-show="formOpen" x-cloak method="POST" action="{{ route('technician.assignments.store') }}" class="grid gap-4 rounded-xl border border-border bg-white p-5 shadow-sm md:grid-cols-2">@csrf<label class="text-sm font-semibold text-heading">Equipment<select name="asset_id" class="{{ $input }}" required>@foreach($equipment as $asset)<option value="{{ $asset->id }}">{{ $asset->reference }} · {{ $asset->serial_number }}</option>@endforeach</select></label><label class="text-sm font-semibold text-heading">Lab<select name="computer_lab_id" class="{{ $input }}"><option value="">No lab assignment</option>@foreach($labs as $lab)<option value="{{ $lab->id }}">{{ $lab->name }}</option>@endforeach</select></label><label class="text-sm font-semibold text-heading">Custodian<input name="custodian" class="{{ $input }}"></label><label class="text-sm font-semibold text-heading">Notes<input name="notes" class="{{ $input }}"></label><button class="rounded-lg bg-busitema-blue px-4 py-2.5 text-sm font-semibold text-white md:col-span-2">Save Assignment</button></form>
<x-technician-table :headers="['Asset ID','Serial Number','Equipment','Current Lab','Custodian','Assigned On']">@forelse($equipment as $asset)<tr><td class="px-5 py-4 font-semibold text-heading">{{ $asset->reference }}</td><td class="px-5 py-4 font-mono text-xs">{{ $asset->serial_number }}</td><td class="px-5 py-4">{{ $asset->name }}</td><td class="px-5 py-4">{{ $asset->computerLab?->name??'Unassigned' }}</td><td class="px-5 py-4">{{ $asset->custodian??'—' }}</td><td class="px-5 py-4">{{ $asset->assignments->first()?->assigned_at?->format('d M Y')??'—' }}</td></tr>@empty<tr><td colspan="6" class="px-5 py-10 text-center">No equipment registered.</td></tr>@endforelse</x-technician-table>
<section class="space-y-3"><h2 class="text-lg font-semibold">Assignment Provenance & History</h2>@forelse($equipment->flatMap->assignments as $assignment)<article class="rounded-xl border border-border bg-white p-4"><div class="mb-3 flex justify-between gap-3"><p class="font-semibold text-heading">{{ $assignment->asset?->reference ?? 'Equipment assignment' }} · Assignment #{{ $assignment->id }}</p><a class="text-sm font-semibold text-busitema-blue" href="{{ route('technician.activity.show',['type'=>'assignments','record'=>$assignment->id]) }}">Full history</a></div><x-record-provenance :record="$assignment" compact /></article>@empty<p class="rounded-xl border border-border bg-white p-5 text-sm text-body-text">No assignment history recorded.</p>@endforelse</section>
@elseif($page==='maintenance')
<div class="rounded-xl border border-blue-200 bg-blue-50 p-4 text-sm text-blue-700">Faults and repairs use the existing Property Management maintenance records. Advance each item through diagnosis, repair, testing and resolution.</div><x-technician-table :headers="['Request','Equipment / Serial','Priority','Technician','Stage','Target','Action']">@forelse($faults as $fault)<tr><td class="px-5 py-4 font-semibold text-heading">{{ $fault->reference }}</td><td class="px-5 py-4">{{ $fault->asset?->reference }}<br><span class="font-mono text-xs">{{ $fault->asset?->serial_number }}</span></td><td class="px-5 py-4">{{ $fault->priority }}</td><td class="px-5 py-4">{{ $fault->assignedTechnician?->name??'Unassigned' }}</td><td class="px-5 py-4">{{ $fault->status }}</td><td class="px-5 py-4">{{ $fault->target_completion_at?->format('d M Y')??'—' }}</td><td class="px-5 py-4"><a class="font-semibold text-busitema-blue" href="{{ route('technician.faults.edit',$fault) }}">Update</a></td></tr>@empty<tr><td colspan="7" class="px-5 py-10 text-center">No active repairs.</td></tr>@endforelse</x-technician-table>
<section class="space-y-3"><h2 class="text-lg font-semibold">Repair Provenance & History</h2>@foreach($faults as $fault)<article class="rounded-xl border border-border bg-white p-4"><div class="mb-3 flex justify-between gap-3"><p class="font-semibold text-heading">{{ $fault->reference }} · {{ $fault->asset?->reference }}</p><a class="text-sm font-semibold text-busitema-blue" href="{{ route('technician.activity.show',['type'=>'faults','record'=>$fault->id]) }}">Full history</a></div><x-record-provenance :record="$fault" compact /></article>@endforeach</section>
@elseif($page==='inspections')
<div class="flex justify-end"><button @click="formOpen=!formOpen" class="rounded-lg bg-busitema-blue px-4 py-2.5 text-sm font-semibold text-white">Record Inspection</button></div><form x-show="formOpen" x-cloak method="POST" action="{{ route('technician.inspections.store') }}" class="grid gap-4 rounded-xl border border-border bg-white p-5 md:grid-cols-2">@csrf<label class="text-sm font-semibold text-heading">Equipment<select name="asset_id" class="{{ $input }}" required>@foreach($equipment as $asset)<option value="{{ $asset->id }}">{{ $asset->reference }} · {{ $asset->serial_number }}</option>@endforeach</select></label><label class="text-sm font-semibold text-heading">Condition<select name="condition" class="{{ $input }}">@foreach(['Good','Fair','Poor','Critical'] as $option)<option>{{ $option }}</option>@endforeach</select></label><label class="text-sm font-semibold text-heading">Operational Status<select name="operational_status" class="{{ $input }}">@foreach(['Operational','Faulty','Under Maintenance','Unassigned','Retired'] as $option)<option>{{ $option }}</option>@endforeach</select></label><label class="text-sm font-semibold text-heading">Next Inspection Due<input type="date" name="next_due_at" class="{{ $input }}"></label><label class="text-sm font-semibold text-heading md:col-span-2">Findings<textarea name="findings" rows="3" class="{{ $input }}" required></textarea></label><button class="rounded-lg bg-busitema-blue px-4 py-2.5 text-sm font-semibold text-white md:col-span-2">Save Inspection</button></form><x-technician-table :headers="['Date','Asset ID','Serial Number','Condition','Status','Technician','Next Due']">@forelse($inspections as $inspection)<tr><td class="px-5 py-4">{{ $inspection->inspected_at->format('d M Y') }}</td><td class="px-5 py-4 font-semibold">{{ $inspection->asset?->reference }}</td><td class="px-5 py-4 font-mono text-xs">{{ $inspection->asset?->serial_number }}</td><td class="px-5 py-4">{{ $inspection->condition }}</td><td class="px-5 py-4">{{ $inspection->operational_status }}</td><td class="px-5 py-4">{{ $inspection->technician?->name }}</td><td class="px-5 py-4">{{ $inspection->next_due_at?->format('d M Y')??'—' }}</td></tr>@empty<tr><td colspan="7" class="px-5 py-10 text-center">No inspections recorded.</td></tr>@endforelse</x-technician-table>
<section class="space-y-3"><h2 class="text-lg font-semibold">Inspection Provenance & History</h2>@foreach($inspections as $inspection)<article class="rounded-xl border border-border bg-white p-4"><div class="mb-3 flex justify-between gap-3"><p class="font-semibold text-heading">Inspection #{{ $inspection->id }} · {{ $inspection->asset?->reference }}</p><a class="text-sm font-semibold text-busitema-blue" href="{{ route('technician.activity.show',['type'=>'inspections','record'=>$inspection->id]) }}">Full history</a></div><x-record-provenance :record="$inspection" compact /></article>@endforeach</section>
@elseif($page==='transfers')
<div class="flex justify-end"><button @click="formOpen=!formOpen" class="rounded-lg bg-busitema-blue px-4 py-2.5 text-sm font-semibold text-white">Request Transfer</button></div><form x-show="formOpen" x-cloak method="POST" action="{{ route('technician.transfers.store') }}" class="grid gap-4 rounded-xl border border-border bg-white p-5 md:grid-cols-2">@csrf<label class="text-sm font-semibold text-heading">Equipment<select name="asset_id" class="{{ $input }}">@foreach($equipment as $asset)<option value="{{ $asset->id }}">{{ $asset->reference }} · {{ $asset->serial_number }}</option>@endforeach</select></label><label class="text-sm font-semibold text-heading">Destination Campus<select name="to_campus_id" class="{{ $input }}">@foreach($campuses as $campus)<option value="{{ $campus->id }}">{{ $campus->name }}</option>@endforeach</select></label><label class="text-sm font-semibold text-heading md:col-span-2">Reason<textarea name="reason" rows="3" class="{{ $input }}" required></textarea></label><button class="rounded-lg bg-busitema-blue px-4 py-2.5 text-sm font-semibold text-white md:col-span-2">Submit Request</button></form><x-technician-table :headers="['Reference','Asset / Serial','From','To','Requested','Status']">@forelse($transfers as $transfer)<tr><td class="px-5 py-4 font-semibold">{{ $transfer->reference }}</td><td class="px-5 py-4">{{ $transfer->asset?->reference }}<br><span class="font-mono text-xs">{{ $transfer->asset?->serial_number }}</span></td><td class="px-5 py-4">{{ $transfer->fromCampus?->name }}</td><td class="px-5 py-4">{{ $transfer->toCampus?->name }}</td><td class="px-5 py-4">{{ $transfer->requested_at->format('d M Y') }}</td><td class="px-5 py-4">{{ $transfer->status }}</td></tr>@empty<tr><td colspan="6" class="px-5 py-10 text-center">No transfer requests.</td></tr>@endforelse</x-technician-table>
<section class="space-y-3"><h2 class="text-lg font-semibold">Transfer Provenance & History</h2>@foreach($transfers as $transfer)<article class="rounded-xl border border-border bg-white p-4"><div class="mb-3 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><p class="font-semibold text-heading">{{ $transfer->reference }} · {{ $transfer->asset?->reference }}</p><div class="flex items-center gap-3">@if(auth()->user()->role?->name === 'System Administrator')<form method="POST" action="{{ route('technician.transfers.update',$transfer) }}" class="flex gap-2">@csrf @method('PATCH')<select name="status" class="rounded-lg border border-border bg-white px-2 py-1.5 text-xs">@foreach(['Requested','Approved','Rejected','Completed'] as $status)<option @selected($transfer->status===$status)>{{ $status }}</option>@endforeach</select><button class="rounded-lg bg-busitema-blue px-3 py-1.5 text-xs font-semibold text-white">Update</button></form>@endif<a class="text-sm font-semibold text-busitema-blue" href="{{ route('technician.activity.show',['type'=>'transfers','record'=>$transfer->id]) }}">Full history</a></div></div><x-record-provenance :record="$transfer" compact /></article>@endforeach</section>
@elseif($page==='documents')
<div class="flex justify-end"><button @click="formOpen=!formOpen" class="rounded-lg bg-busitema-blue px-4 py-2.5 text-sm font-semibold text-white">Upload Document</button></div><form x-show="formOpen" x-cloak method="POST" enctype="multipart/form-data" action="{{ route('technician.documents.store') }}" class="grid gap-4 rounded-xl border border-border bg-white p-5 md:grid-cols-2">@csrf<label class="text-sm font-semibold text-heading">Equipment<select name="asset_id" class="{{ $input }}">@foreach($equipment as $asset)<option value="{{ $asset->id }}">{{ $asset->reference }} · {{ $asset->serial_number }}</option>@endforeach</select></label><label class="text-sm font-semibold text-heading">Document Type<input name="document_type" class="{{ $input }}" placeholder="Manual, warranty, quotation..." required></label><label class="text-sm font-semibold text-heading">Title<input name="title" class="{{ $input }}" required></label><label class="text-sm font-semibold text-heading">Expiry Date<input type="date" name="expires_at" class="{{ $input }}"></label><label class="text-sm font-semibold text-heading md:col-span-2">File (max 10 MB)<input type="file" name="document" class="{{ $input }}" required></label><button class="rounded-lg bg-busitema-blue px-4 py-2.5 text-sm font-semibold text-white md:col-span-2">Upload Document</button></form><x-technician-table :headers="['Reference','Document','Type','Asset / Serial','Expiry','Action']">@forelse($documents as $document)<tr><td class="px-5 py-4 font-semibold">{{ $document->reference }}</td><td class="px-5 py-4">{{ $document->title }}</td><td class="px-5 py-4">{{ $document->document_type }}</td><td class="px-5 py-4">{{ $document->asset?->reference }}<br><span class="font-mono text-xs">{{ $document->asset?->serial_number }}</span></td><td class="px-5 py-4">{{ $document->expires_at?->format('d M Y')??'—' }}</td><td class="px-5 py-4"><a href="{{ route('technician.documents.download',$document) }}" class="font-semibold text-busitema-blue">Download</a></td></tr>@empty<tr><td colspan="6" class="px-5 py-10 text-center">No technical documents uploaded.</td></tr>@endforelse</x-technician-table>
<section class="space-y-3"><h2 class="text-lg font-semibold">Document Provenance & History</h2>@foreach($documents as $document)<article class="rounded-xl border border-border bg-white p-4"><div class="mb-3 flex justify-between gap-3"><p class="font-semibold text-heading">{{ $document->reference }} · {{ $document->title }}</p><a class="text-sm font-semibold text-busitema-blue" href="{{ route('technician.activity.show',['type'=>'documents','record'=>$document->id]) }}">Full history</a></div><x-record-provenance :record="$document" compact /></article>@endforeach</section>
@elseif($page==='reports')
<div class="flex justify-end"><a href="{{ route('technician.reports.export') }}" class="rounded-lg bg-busitema-blue px-4 py-2.5 text-sm font-semibold text-white">Export Equipment CSV</a></div><section class="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">@foreach([['Registered Equipment',$equipment->count()],['Operational',$equipment->where('operational_status','Operational')->count()],['Faulty',$equipment->where('operational_status','Faulty')->count()],['Warranty ≤ 90 days',$equipment->filter(fn($asset)=>$asset->warranty_expires_at?->between(today(),today()->addDays(90)))->count()]] as [$label,$value])<article class="rounded-xl border border-border bg-white p-5"><p class="text-sm text-body-text">{{ $label }}</p><p class="mt-3 text-3xl font-semibold text-heading">{{ $value }}</p></article>@endforeach</section><x-technician-table :headers="['Asset ID','Serial Number','Type','Campus','Lab','Condition','Status','Warranty']">@forelse($equipment as $asset)<tr><td class="px-5 py-4 font-semibold">{{ $asset->reference }}</td><td class="px-5 py-4 font-mono text-xs">{{ $asset->serial_number }}</td><td class="px-5 py-4">{{ $asset->type?->name }}</td><td class="px-5 py-4">{{ $asset->campus?->name }}</td><td class="px-5 py-4">{{ $asset->computerLab?->name??'—' }}</td><td class="px-5 py-4">{{ $asset->condition }}</td><td class="px-5 py-4">{{ $asset->operational_status }}</td><td class="px-5 py-4">{{ $asset->warranty_expires_at?->format('d M Y')??'—' }}</td></tr>@empty<tr><td colspan="8" class="px-5 py-10 text-center">No report data available.</td></tr>@endforelse</x-technician-table>
@endif</div>
@endsection
